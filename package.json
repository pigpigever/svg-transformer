{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "npx lerna run build",
    "tag": "npx lerna version --no-private",
    "publish": "npx lerna publish --no-private"
  },
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
