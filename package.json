{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "lerna run build",
    "version": "lerna version --conventional-commits --no-push --no-git-tag-version",
    "version:beta":"lerna version prerelease --preid beta --no-push --no-git-tag-version",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
