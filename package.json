{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "npx lerna run build",
    "tag": "npx lerna version"
  },
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
