{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "lerna run build",
    "tag": "lerna version",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
